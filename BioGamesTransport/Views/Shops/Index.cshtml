@model IEnumerable<BioGamesTransport.Data.SQL.Shops>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BaseUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastSyncOrderId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastSync)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastSyncProdId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BaseUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastSyncOrderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastSync)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastSyncProdId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
